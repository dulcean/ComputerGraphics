# Code Review Document

## 📝 Overview

**Дата**: 10.11.2024
**Автор**: Dulcean
**Описание**:

- Написать приложение/веб-приложение, иллюстрирующее работу базовых растровых алгоритмов
- На проверку сдаются: exe, который должен работать на любом ПК под Windows XP /веб-приложение, выложенное в общий доступ; исходный код; сопроводительная документация.

## 🧐 Key Areas to Review

- Codestyle
- Project structure
- GPT usage
- Code performance
- Algorithms performance

## 📝 Summary

**Общий обзор**: 3 файла:

- 1 ui
- 1 stresstest
- 1 алгоритмы

## ✅ Positive Aspects

- Структурированность:
  - Есть комментарии, отступы под алгоритмами, код чистый
  - Конкретные названия

- Работоспособность:
  - Тесты рабочие, время схоже с оригиналом (если брать за эталон какой-либо из алгоритмов)

## 🔍 Detailed Code Review

#### 1. Как было описано ранее у нас есть `algorithms.py` `main.py` `testing.py`

Рассмотрим каждый по-отдельности. Algorithms.py разбит на 4 метода. Методы сами по себе возвращают массив точек.
Нет реализации дополнительных алгоритмов.
Step by step алгоритм не будет банально работать в случаях захода по направлению шага и все это обернуто в abs

```py
  if (abs(x2 - x1) >= abs(y2 - y1)):
        a = (y2 - y1) / (x2 - x1)
```

Также можем ловить деление на ноль

#### 2. Dda_line также делит на ноль

#### 3. В линии Брезенхема последняя точка может не быть добавлена в список результатов

```py
    else:
        err = Dy // 2
        while y != y2:
            points.append((x, y))
            err -= Dx
            if err < 0:
                x += Sx
                err += Dy
            y += Sy
    points.append((x2, y2))  # Пускай сюда точку добавим
```

#### 4. В окружности все норм, я бы убрал проверку `x < y break`

#### 5. Перейдем к RasterizationInterface

Желательно выносить поля ввода и компоненту графика в другие модули чтоб не засорять мэйн апп класс.
Парсится флот, возможна ошибка

```py
  x1 = float(self.entry_x1.get())
  y1 = float(self.entry_y1.get())
  x2 = float(self.entry_x2.get())
  y2 = float(self.entry_y2.get())
```

#### 6. testing

Единственная ошибка - у Пошагового алгоритма будет ложное время, поле на первой итерации будет подгружать твой рисователь графиков.

## ⚠️ Improvement Suggestions

Улучшения:

- **Оптимизация**:

  - ```py
    def test_algorithms(iterations=100):
    step_by_step_times = []
    dda_times = []
    bresenham_line_times = []
    bresenham_circle_times = []
    ```

  - ```py
    class RasterizationInterface:
    def __init__(self, root):
    ```

- **Архитектура**:
  - Использование классов, ооп а не писать все в один класс.

## 🎯 Summary and Action Points

**Общие выводы**: Код рабочий, но требуются корректировки

**GPT Usage**: 33%

**Статус ревью**:  _Требуются доработки_
